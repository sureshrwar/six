# /etc/rc

sleep 0.25
echo Executing /etc/rc.

sleep 0.25

/bin/mount_all
/bin/configure_dm

sleep 0.25

# Hostname is initialized by kernel (-H flag, SIX_HOSTNAME env, or default 'black')
echo Setting hostname to `hostname`

sleep 0.25

# Start HTTP server
echo Starting a local HTTP server.
httpd &

sleep 0.25

# Start Telnet daemon
echo Starting Telnet daemon.
telnetd &

sleep 0.25

sixanim
sleep 0.25
echo Escape character is '^\\'.

sleep 0.25
